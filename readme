sudo systemctl daemon-reload

# To stop ngrok
sudo systemctl stop ngrok

# To run ngrok
sudo systemctl start ngrok

#To run permanently
journalctl -u ngrok -f --no-pager

#To get current ngrok url
curl -s http://127.0.0.1:4040/api/tunnels | jq -r '.tunnels[0].public_url'